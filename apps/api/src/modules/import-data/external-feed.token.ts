/** DI token for the external master-data feed (IDoc / API) adapter. */
export const EXTERNAL_FEED_ADAPTER = 'EXTERNAL_FEED_ADAPTER';
export type { ExternalFeedAdapter, ExternalFeedConfig } from './external-feed.adapter';
