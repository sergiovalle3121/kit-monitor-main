{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --include=dev && npm run build --workspace=axos-os-backend"
  },
  "deploy": {
    "startCommand": "npm run start:prod --workspace=axos-os-backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
